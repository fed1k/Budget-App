<a href="/users/<%=current_user.id%>/groups/<%= group.id%>" id="<%= dom_id group %>" class="category-card">
  <img src="https://source.unsplash.com/random/1920x1080/?<%=group.icon%>" />
  
  <p class="category-div-name">
    <%= group.name %>
  </p>

  <p class="total-amout-text">$<%=Entity.where(group_id: group.id).sum(:amount)%></p>

</a>
